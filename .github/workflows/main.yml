name: ci

on: [push]

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ls -la
  build:
    runs-on: ubuntu-latest
    needs: checkout
    steps:
      - run: echo "buildingg code"
      - run: echo "testing code"
      - run: ls -la
    
